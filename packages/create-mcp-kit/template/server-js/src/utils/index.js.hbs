export function getOptions(argv, pkg) {
  return {
    name: pkg.name,
    version: pkg.version,
{{#if (or (includes transports 'streamable') (includes transports 'sse'))}}
    port: argv.port,
{{/if}}
  }
}
